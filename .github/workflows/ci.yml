name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

<<<<<<< HEAD
      - name: Run pytest; show current date
        run: python -m pytest -s -q
=======
      - name: Add Jest tests and CI test job; show current date
        run: npm test -- --ci
>>>>>>> bf5bb3d61976898a2c12d333f65ee1311de8d09a
